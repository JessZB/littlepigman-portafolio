{
  "name": "littlepigman-portafolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "setup:agents": "git submodule update --init --recursive && node _shared_agents/common/setup-agents.js",
    "postinstall": "npm run setup:agents"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "concurrently": "^9.2.1"
  }
}
